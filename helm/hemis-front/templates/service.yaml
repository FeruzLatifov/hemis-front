apiVersion: v1
kind: Service
metadata:
  name: {{ include "hemis-front.fullname" . }}
  labels:
    {{- include "hemis-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hemis-front.selectorLabels" . | nindent 4 }}
